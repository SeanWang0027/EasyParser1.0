// Function definition
int sum(int a, int b) {
    // Variable declaration
    int result;

    // Expression statement (calculating the sum)
    result = a + b;

    // Return statement
    return result;
}

int main() {
    // Call the sum function with two numbers
    sum(10, 20);

    return 0;
}
